# Loaded automatically by `vite build` / `npm run build:prod` (Selise Blocks Docker uses this file from the repo).
# VITE_* is embedded in the client bundle. X-Blocks-Key is scoped to your Blocks project; rotate in Cloud if the repo is leaked.
VITE_API_BASE_URL=https://api.seliseblocks.com
VITE_BLOCKS_API_URL=https://api.seliseblocks.com
VITE_X_BLOCKS_KEY=Pec75a81a0cdf4683bb82bc30fbccb214
VITE_PROJECT_SLUG=pemfes
# Optional fallback JWT (client_credentials) if Data Gateway View is not Public; see DEPLOYMENT.md. Prefer Schema Access → View → Public for VibeWebsite / VibePage / VibeAsset.
# VITE_VIBE_PUBLIC_READ_TOKEN=
VITE_CAPTCHA_SITE_KEY=
VITE_CAPTCHA_TYPE=
